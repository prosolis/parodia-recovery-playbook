# Secrets & state — never commit
compose/.env
compose/synapse/homeserver.signing.key
compose/data/

# Synapse config carries live secrets once filled (password + the 3 etke secrets)
compose/synapse/homeserver.yaml
