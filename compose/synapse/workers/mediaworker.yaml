worker_app: synapse.app.generic_worker
worker_name: mediaworker
worker_listeners:
  - type: http
    port: 8008
    x_forwarded: true
    resources:
      - names: [media]
worker_log_config: /data/log.config
